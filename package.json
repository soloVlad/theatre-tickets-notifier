{
  "name": "theatre-tickets-notifier",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "lint": "biome check src",
    "format": "biome format src --write && biome lint src --write"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "@biomejs/biome": "^1.9.4",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
